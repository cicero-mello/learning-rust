fn main() {
    println!("Hello, world!");
}

// para compilar: rustc main.rs (já estando na pasta "hello-world")
/*
    Isso vai gerar um arquivo binario "main" (sem extensão).
*/

// para executar o build: ./main

/*
    Obs: RUST possui baixa sinergia com Windows, portanto,
    instalei uma distro Linux (Ubuntu) e configurei a linguagem
    na máquina pelo WSL. Ou seja, todos os comandos falados
    consideram que o projeto está em Linux (não Windows).
*/
